/**
 * Enumeration with the available log levels.
 */
export enum LogLevel {
  Debug = 'debug',
  Info = 'info',
  Notice = 'notice',
  Warning = 'warning',
  Error = 'error',
  Emergency = 'emergency',
}
