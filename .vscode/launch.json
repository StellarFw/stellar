{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Example App",
      "program": "${workspaceFolder}/bin/stellar",
      "args": [
        "run"
      ],
      "cwd": "${workspaceFolder}/example"
    }
  ]
}
