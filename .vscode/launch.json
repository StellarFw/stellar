{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node",
			"request": "launch",
			"name": "Stellar Debug",
			"program": "${workspaceFolder}/bin/stellar run",
			"preLaunchTask": "npm: build",
			"outFiles": ["${workspaceFolder}/src/**/*.js"],
			"outputCapture": "std"
		}
	]
}
